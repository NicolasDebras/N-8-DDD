import { Player } from "../models/Player";
/*
export class DayTurn {

    allPlayer: Player[];
    deadPlayers : Player[];

    constructor (allPlayer: Player[], deadPlayers : Player[]) {
        this.allPlayer = allPlayer;
        this.deadPlayers =  deadPlayers )
    }

    startDay() { // je ne sais pas quoi return 

        // Methode qui regarde si c'est pas la fin de la parie
        checkVictory();

        this.deadPlayers.forEach((element : Player) => {
            if (element.role.name = ERoles.HUNTER) {
                player.role.ShootHunter()
            } 
            else if (element.IsInLove()) {
                //aucun idee de comment recuperer l'autre 
            } 
            else if (element.IsMayor == true) {
                element.selectNewMayor()
            }
        });

        // re check 
        checkVictory();

        //vote 
        VillagerVote(this.allPlayer)

        //return un truc 

    }

    

}
*/