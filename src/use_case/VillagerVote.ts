import { Player } from "../models/Player";

/*
export class VillagerVote {
    
    vote(playerList: Player[]){
        let voteList:string[]=[]
        playerList.forEach(player => voteList.push(player.getVote(playerList)))

        let res=voteList.getMostFrequent()
        if(res.length==1){
            return res[0]
        }else{
            list
        }
    }
}
*/